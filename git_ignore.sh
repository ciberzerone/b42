#!/bin/bash
   
   # Lista todos los archivos ignorados por git que actualmente existen en tu repositorio local.
   git ls-files --others --ignored --exclude-standard
   ciberzerone@gmail.com.pub
   .DS_Store

   
